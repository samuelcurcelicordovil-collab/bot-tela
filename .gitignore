# NUNCA suba isto: contem a senha do TURN.
# No servidor hospedado, use variaveis de ambiente no painel.
turn.json

node_modules/
npm-debug.log*
.env

# Senha de entrada — NUNCA subir para o Git
senha.txt

quedas.log

# Chave da API do YouTube (busca de musica) — NUNCA subir para o Git
youtube.txt

# Configuracao local do editor/agente, nao e do projeto
.claude/
